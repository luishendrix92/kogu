/**
 * TODO: Describe
 * @param a
 * @param b
 */
export function and(a: boolean, b: boolean): boolean {
  return a && b;
}

/**
 * TODO: Describe
 * @param a
 * @param b
 */
export function or(a: boolean, b: boolean): boolean {
  return a || b;
}
