/**
 * TODO: Implement prune!
 * Truncates a string, doesn't exceed original length and
 * ensures that there are no incomplete words.
 * @param text
 */
export default function prune(text: string): string {
  return text;
}
